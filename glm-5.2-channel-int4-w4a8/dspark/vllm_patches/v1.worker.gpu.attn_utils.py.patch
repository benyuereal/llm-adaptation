--- a/vllm/v1/worker/gpu/attn_utils.py
+++ b/vllm/v1/worker/gpu/attn_utils.py
@@ -1,5 +1,6 @@
 # SPDX-License-Identifier: Apache-2.0
 # SPDX-FileCopyrightText: Copyright contributors to the vLLM project
+from collections import defaultdict
 from collections.abc import Sequence
 from typing import Any, cast
 
@@ -16,8 +17,9 @@
     AttentionSpec,
     KVCacheConfig,
     KVCacheSpec,
+    UniformTypeKVCacheSpecs,
 )
-from vllm.v1.worker.utils import bind_kv_cache
+from vllm.v1.worker.utils import AttentionGroup, bind_kv_cache
 
 
 def get_kv_cache_spec(vllm_config: VllmConfig) -> dict[str, KVCacheSpec]:
@@ -36,33 +38,76 @@
     vllm_config: VllmConfig,
     device: torch.device,
 ):
+    """Initialize attention backends and per-attn-group metadata builders.
+
+    Backported: a single KV-cache group (e.g. a ``UniformTypeKVCacheSpecs``
+    group that bundles the GLM-5.2 target's MLA attention layers and DSA
+    indexer layers, which share the same spec *type* but use *different*
+    attention backends) may contain layers that need distinct attention
+    metadata. We therefore split each KV-cache group into sub-groups keyed by
+    ``(backend, per-layer kv_cache_spec)`` -- mirroring the full
+    ``gpu_model_runner`` -- and create one metadata builder per sub-group.
+    """
     attn_backends: dict[str, type[AttentionBackend]] = {}
-    attn_metadata_builders: list[AttentionMetadataBuilder] = []
+    # attn_groups[kv_cache_group_id] = list[AttentionGroup]
+    attn_groups: list[list[AttentionGroup]] = []
     flashinfer_workspace: torch.Tensor | None = None
-    for kv_cache_group_spec in kv_cache_config.kv_cache_groups:
+
+    layer_type = cast(type[Any], AttentionLayerBase)
+    for kv_cache_gid, kv_cache_group_spec in enumerate(
+        kv_cache_config.kv_cache_groups
+    ):
         layer_names = kv_cache_group_spec.layer_names
-        any_layer_name = next(iter(layer_names))
-
-        layer_type = cast(type[Any], AttentionLayerBase)
-        attn_layers = get_layers_from_vllm_config(vllm_config, layer_type, layer_names)
-        attn_backend = attn_layers[any_layer_name].get_attn_backend()
+        attn_layers = get_layers_from_vllm_config(
+            vllm_config, layer_type, layer_names
+        )
+
+        # Dedupe by (full class name, per-layer kv_cache_spec) so that layers
+        # sharing a backend+spec reuse one metadata builder, while layers with
+        # a different backend (MLA attn vs DSA indexer) get their own.
+        groups_by_key: dict[
+            tuple[str, KVCacheSpec], tuple[Any, KVCacheSpec, list[str]]
+        ] = {}
+        key_order: list[tuple[str, KVCacheSpec]] = []
         for layer_name in layer_names:
+            attn_backend = attn_layers[layer_name].get_attn_backend()
             attn_backends[layer_name] = attn_backend
 
-        attn_metadata_builder = attn_backend.get_builder_cls()(
-            kv_cache_group_spec.kv_cache_spec,
-            layer_names,
-            vllm_config,
-            device,
-        )
-        attn_metadata_builders.append(attn_metadata_builder)  # type: ignore
-
-        if attn_backend.get_name() == "FLASHINFER":
-            if flashinfer_workspace is None:
-                flashinfer_workspace = attn_metadata_builder._get_workspace_buffer()
-            else:
-                attn_metadata_builder.set_workspace_buffer(flashinfer_workspace)
-    return attn_backends, attn_metadata_builders
+            layer_kv_cache_spec = kv_cache_group_spec.kv_cache_spec
+            if isinstance(layer_kv_cache_spec, UniformTypeKVCacheSpecs):
+                layer_kv_cache_spec = layer_kv_cache_spec.kv_cache_specs[layer_name]
+            key = (attn_backend.full_cls_name(), layer_kv_cache_spec)
+            if key not in groups_by_key:
+                groups_by_key[key] = (attn_backend, layer_kv_cache_spec, [])
+                key_order.append(key)
+            groups_by_key[key][2].append(layer_name)
+
+        sub_groups: list[AttentionGroup] = []
+        for key in key_order:
+            attn_backend, layer_kv_cache_spec, sub_layer_names = groups_by_key[key]
+            attn_group = AttentionGroup(
+                attn_backend,
+                sub_layer_names,
+                layer_kv_cache_spec,
+                kv_cache_gid,
+            )
+            attn_group.create_metadata_builders(
+                vllm_config,
+                device,
+                kernel_block_size=None,
+                num_metadata_builders=1,
+            )
+            sub_groups.append(attn_group)
+
+            if attn_backend.get_name() == "FLASHINFER":
+                builder = attn_group.get_metadata_builder(0)
+                if flashinfer_workspace is None:
+                    flashinfer_workspace = builder._get_workspace_buffer()
+                else:
+                    builder.set_workspace_buffer(flashinfer_workspace)
+        attn_groups.append(sub_groups)
+
+    return attn_backends, attn_groups
 
 
 def _allocate_kv_cache(
@@ -93,37 +138,112 @@
     kv_caches: dict[str, torch.Tensor] = {}
     for kv_cache_group_spec in kv_cache_config.kv_cache_groups:
         kv_cache_spec = kv_cache_group_spec.kv_cache_spec
-        assert isinstance(kv_cache_spec, AttentionSpec)
+        # Backported: the uniform-type single-group path wraps per-layer specs
+        # in a UniformTypeKVCacheSpecs (e.g. MLA target + draft sharing one
+        # group when hybrid KV cache manager is disabled). Each layer then has
+        # its own page size / shape, so resolve the per-layer spec.
+        if isinstance(kv_cache_spec, UniformTypeKVCacheSpecs):
+            per_layer_specs = kv_cache_spec.kv_cache_specs
+        else:
+            assert isinstance(kv_cache_spec, AttentionSpec)
+            per_layer_specs = None
         for layer_name in kv_cache_group_spec.layer_names:
+            layer_spec = (
+                per_layer_specs[layer_name]
+                if per_layer_specs is not None
+                else kv_cache_spec
+            )
+            assert isinstance(layer_spec, AttentionSpec)
             raw_tensor = kv_cache_raw_tensors[layer_name]
-            assert raw_tensor.numel() % kv_cache_spec.page_size_bytes == 0
-            num_blocks = raw_tensor.numel() // kv_cache_spec.page_size_bytes
+            assert raw_tensor.numel() % layer_spec.page_size_bytes == 0
+            num_blocks = raw_tensor.numel() // layer_spec.page_size_bytes
 
             attn_backend = attn_backends[layer_name]
             kv_cache_shape = attn_backend.get_kv_cache_shape(
                 num_blocks,
-                kv_cache_spec.block_size,
-                kv_cache_spec.num_kv_heads,
-                kv_cache_spec.head_size,
+                layer_spec.block_size,
+                layer_spec.num_kv_heads,
+                layer_spec.head_size,
+            )
+
+            # Backported: some ROCm flash-attention backends return *split*
+            # (key_shape, value_shape) -- a tuple of two shape tuples -- rather
+            # than a single flat shape. Detect that structurally (each element
+            # is itself a sequence of ints) and handle the two halves
+            # separately, mirroring gpu_model_runner._reshape_kv_cache_tensors.
+            is_split_kv = (
+                isinstance(kv_cache_shape, (tuple, list))
+                and len(kv_cache_shape) == 2
+                and all(
+                    isinstance(s, (tuple, list))
+                    and all(isinstance(d, int) for d in s)
+                    for s in kv_cache_shape
+                )
             )
 
             # FIXME(woosuk): Add kv_cache_stride_order to all attention backends.
             try:
                 kv_cache_stride_order = attn_backend.get_kv_cache_stride_order()
+            except (AttributeError, NotImplementedError):
+                kv_cache_stride_order = None
+
+            dtype = layer_spec.dtype
+            raw_tensor = raw_tensor.view(dtype)
+
+            if is_split_kv:
+                key_cache_shape, value_cache_shape = kv_cache_shape
+                if kv_cache_stride_order is None:
+                    key_stride_order = tuple(range(len(key_cache_shape)))
+                    value_stride_order = tuple(range(len(value_cache_shape)))
+                else:
+                    key_stride_order, value_stride_order = kv_cache_stride_order
+                assert len(key_stride_order) == len(key_cache_shape)
+                assert len(value_stride_order) == len(value_cache_shape)
+                key_cache_shape = tuple(
+                    key_cache_shape[i] for i in key_stride_order
+                )
+                value_cache_shape = tuple(
+                    value_cache_shape[i] for i in value_stride_order
+                )
+                inv_key_order = [
+                    key_stride_order.index(i) for i in range(len(key_stride_order))
+                ]
+                inv_value_order = [
+                    value_stride_order.index(i)
+                    for i in range(len(value_stride_order))
+                ]
+                total_elements = raw_tensor.numel()
+                key_elements = 1
+                for d in key_cache_shape:
+                    key_elements *= d
+                value_elements = 1
+                for d in value_cache_shape:
+                    value_elements *= d
+                assert total_elements == key_elements + value_elements
+                key_cache = (
+                    raw_tensor[:key_elements]
+                    .view(key_cache_shape)
+                    .permute(*inv_key_order)
+                )
+                value_cache = (
+                    raw_tensor[key_elements:]
+                    .view(value_cache_shape)
+                    .permute(*inv_value_order)
+                )
+                kv_caches[layer_name] = (key_cache, value_cache)
+            else:
+                if kv_cache_stride_order is None:
+                    kv_cache_stride_order = tuple(range(len(kv_cache_shape)))
                 assert len(kv_cache_stride_order) == len(kv_cache_shape)
-            except (AttributeError, NotImplementedError):
-                kv_cache_stride_order = tuple(range(len(kv_cache_shape)))
-
-            kv_cache_shape = tuple(kv_cache_shape[i] for i in kv_cache_stride_order)
-            inv_order = [
-                kv_cache_stride_order.index(i)
-                for i in range(len(kv_cache_stride_order))
-            ]
-
-            dtype = kv_cache_spec.dtype
-            raw_tensor = raw_tensor.view(dtype)
-            raw_tensor = raw_tensor.view(kv_cache_shape)
-            kv_caches[layer_name] = raw_tensor.permute(*inv_order)
+                kv_cache_shape = tuple(
+                    kv_cache_shape[i] for i in kv_cache_stride_order
+                )
+                inv_order = [
+                    kv_cache_stride_order.index(i)
+                    for i in range(len(kv_cache_stride_order))
+                ]
+                raw_tensor = raw_tensor.view(kv_cache_shape)
+                kv_caches[layer_name] = raw_tensor.permute(*inv_order)
     return kv_caches
 
 
@@ -153,7 +273,7 @@
 
 
 def build_attn_metadata(
-    attn_metadata_builders: list[AttentionMetadataBuilder],
+    attn_groups: list[list[AttentionGroup]],
     num_reqs: int,
     num_tokens: int,
     query_start_loc_gpu: torch.Tensor,
@@ -163,15 +283,24 @@
     block_tables: Sequence[torch.Tensor],
     slot_mappings: torch.Tensor,
     kv_cache_config: KVCacheConfig,
+    for_cudagraph_capture: bool = False,
 ) -> dict[str, Any]:
+    """Build per-layer attention metadata.
+
+    Backported: iterates over the per-KV-cache-group attention sub-groups
+    (``attn_groups``) so that layers sharing a KV-cache group but using
+    different attention backends each receive metadata built by their own
+    backend's builder. ``block_tables``/``slot_mappings`` are still indexed by
+    KV-cache group id (shared across that group's attn sub-groups).
+    """
     max_query_len = int(query_start_loc_cpu.max())
     seq_lens = seq_lens[:num_reqs]
 
     attn_metadata: dict[str, Any] = {}
     kv_cache_groups = kv_cache_config.kv_cache_groups
-    for i, kv_cache_spec in enumerate(kv_cache_groups):
-        block_table = block_tables[i]
-        slot_mapping = slot_mappings[i]
+    for kv_cache_gid, kv_cache_group_spec in enumerate(kv_cache_groups):
+        block_table = block_tables[kv_cache_gid]
+        slot_mapping = slot_mappings[kv_cache_gid]
 
         common_attn_metadata = CommonAttentionMetadata(
             query_start_loc=query_start_loc_gpu,
@@ -186,11 +315,17 @@
             causal=True,
         )
 
-        attn_metadata_builder = attn_metadata_builders[i]
-        metadata = attn_metadata_builder.build(
-            common_prefix_len=0,
-            common_attn_metadata=common_attn_metadata,
-        )
-        for layer_name in kv_cache_spec.layer_names:
-            attn_metadata[layer_name] = metadata
+        for attn_group in attn_groups[kv_cache_gid]:
+            builder = attn_group.get_metadata_builder(0)
+            if for_cudagraph_capture:
+                metadata = builder.build_for_cudagraph_capture(
+                    common_attn_metadata
+                )
+            else:
+                metadata = builder.build(
+                    common_prefix_len=0,
+                    common_attn_metadata=common_attn_metadata,
+                )
+            for layer_name in attn_group.layer_names:
+                attn_metadata[layer_name] = metadata
     return attn_metadata
